# 航天日报最小直连定时推送实施计划

1. 将 `daily-intelligence.yml` 的 schedule 收敛为北京时间 07:50 对应的唯一 cron，保留直接运行生产 job 的结构。
2. 删除 workflow 内每日完整测试步骤，将依赖安装从开发依赖改为生产依赖，同时保留生产门禁、采集、报告、钉钉投递和诊断步骤。
3. 更新 workflow 静态测试，使其明确约束唯一 cron、无每日 pytest 步骤、只安装生产依赖及既有投递模式不变。
4. 在本地安装开发依赖并运行完整测试、YAML 解析检查和 `git diff --check`，确保内容生成链路未被修改。
5. 更新进度文档，提交改造并推送 `main`；不手动触发 Actions，不发送钉钉，等待下一次北京时间 07:50 自动运行验收。
