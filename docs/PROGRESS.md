# 激光与商业航天情报日报项目进度

更新时间：2026-07-30（北京时间）

## 当前结论

```text
博查真实响应解析：已修复并合并到 main
真实候选采集：已恢复，最近一次安全运行返回 39 条
候选可见性：PR #4 已创建，尚未合并
“有无信息”PRD：已确认
“有无信息”开发：已推送并创建 PR #5，尚未合并
新版本真实 dry-run：已执行，搜索门槛通过但暴露候选展示缺陷
候选展示缺陷：已修复并通过真实钉钉试发验证
离线测试：475 passed
钉钉真实试发：已成功执行一次；一次性分支已恢复 dry-run
定时任务：当前未启用 schedule
仓库可见性：public，未修改
智能多轮检索：40 查询历史回填 dry-run 已完成，正在进行候选质量收敛
候选核验晋级：最新真实 dry-run 仍为 0 条已核实；页面日期、主体归属、三目标覆盖和 B 级来源优先修复已完成离线验证
```

## 已确认事实

- 2026-07-25、2026-07-26 的 scheduled Actions 在北京时间约 15:30、15:58 运行，可能已发送空钉钉日报。
- 目标时间为北京时间 07:30，但历史实际触发时间不符合目标。
- Actions 显示 success 只代表进程成功，不代表采集业务成功。
- 博查真实结果位于 `data.webPages.value`。
- PR #3 已合并，合并提交为 `eef32c8`。
- 当前主 workflow 只保留手动入口，强制 dry-run，不包含 schedule，不提交运行状态，不发送钉钉。
- 最近一次安全验证运行使用 4 个核心查询，返回 39 条候选、5 条待核实、0 条已核实，报告能够展示标题、摘要和链接。
- 2026-07-27 的 PR #5 安全验证运行使用 4 个核心查询，博查原始 40 条、结构有效 40 条、主题相关 10 条，最终候选 5 条。
- 该次运行的 5 条最终候选在后续分析/核验后未进入正式数据或 `pending`，导致 Artifact 仍显示空日报；这是“Actions 成功但业务失败”的一次真实实例。
- 候选丢失原因已定位：流水线保留了漏斗计数，但日报只读取正式数据和 `pending`，下游判为 `REJECTED` 的搜索候选被直接跳过。
- 2026-07-27 Actions #5 从一次性分支 `codex/dingtalk-test-20260727` 成功完成真实采集和钉钉试发，运行提交为 `5eba7b7`。
- 钉钉消息成功展示 5 条“搜索候选（未核实）”，证明采集、报告和通知链路已经贯通。
- 一次性测试分支已通过提交 `688d413` 恢复为强制 dry-run，后续误点不会再次发送。
- 本次 5 条候选质量仍不达标：1 条融资综述有一定价值；2 条行业研究报告高度重复；1 条为股市评论；1 条为券商观点。
- 其他业务板块仍为空是因为它们只展示已核实事件；本次已核实 0、待核实 0，不应把搜索候选伪装成正式情报。
- 候选正文失败的主要外部原因包括人机验证、TLS 证书链错误和 HTTP 521。
- 仓库当前为 public。
- 2026-07-28 的智能历史回填 Actions 已完成；该分支强制 `--dry-run`，使用无效占位 webhook，因此没有也不可能发送钉钉。
- 本次回填博查原始 218 条、主题相关 59 条、最终候选 21 条、待核实 3 条；智能检索实际调用 27 次并正常停止。
- 回填证明检索智能体可以发现具体采购项目和商业航天融资，但暴露出研报售卖页、投融周报、跨媒体重复融资及 90 天标签错误。
- 质量修复后的第二次回填博查原始 144 条、主题相关 33 条、事件级合并 10 条，但 90 天内最终候选仅 1 条；研报和重复噪声已明显消失，召回率未达标。
- 第二次回填的智能体实际调用 19/40 次，在第 4 轮达到轮次上限；研究轨迹显示其多次追查 2025 年窗口外旧项目。
- 第三次回填博查原始 216 条、主题相关 32 条、最终候选 7 条；时间窗口、轮次和数量门槛通过。
- 第三次回填仍有 2 条新闻早/晚报聚合页和 1 条产业综述，7 条中约 43% 不是单一融资或采购事件，因此未进入钉钉试发。
- 第三次回填后的单一融资事件口径已完成离线修复；回填验证阶段结束，进入 12 查询日常强制 dry-run。
- 12 查询日常 dry-run 返回博查原始 84 条、主题相关 9 条、最终候选 5 条；没有研报或聚合资讯噪声，数量门槛通过。
- 日常候选中光邮星空同一次“Pre-A 和 Pre-A+”连续融资仍以 3 个来源重复展示；已改为按企业和融资轮次集合重叠进行事件合并。
- 2026-07-28 智能检索版完成一次 12 查询真实钉钉测试发送，标题带“【测试】”；博查原始 98 条、主题相关 23 条、最终候选 5 条。
- 真实消息确认采集、智能检索、报告渲染、钉钉签名和通知链路贯通；一次性测试分支随后恢复为强制 dry-run。
- 消息中微光启航天使++轮仍有 2 条跨媒体重复，说明公司简称、法律全称和标题前缀的归一化仍需加强。
- 已统一“火箭新锐公司微光启航”“北京微光启航科技有限公司”和“微光启航”等企业名称变体。
- “已核实 0”的核心代码原因已定位：搜索阶段在核验前删除了同一事件的跨媒体来源，导致“两家独立 B 级来源”规则没有第二来源可用。
- DeepSeek 候选分析降级后的规则分析器原先不提取融资企业、轮次和金额；同时页面标题虽属于允许事实，证据落地检查却只接受正文，可能把有效模型结果误判为降级。
- 开发分支现保留每个入选事件最多 2 条备用来源并优先不同域名；日报仍只展示一个事件候选，不会恢复重复刷屏。
- 页面标题证据现可参与落地核验；规则降级现可保守提取融资企业、披露日期、轮次、金额及未披露状态。
- 正式晋级口径未放宽：招采仍要求 A 级来源；融资仍要求企业/投资方官方来源，或两个独立 B 级来源且关键字段一致；C 级和证据不足仍只进入待核实。

## 候选核验晋级开发

当前开发分支：

```text
codex/verification-promotion-20260728
```

已完成：

- [x] 同事件搜索结果先按时间窗筛选，避免旧转载遮蔽近期来源。
- [x] 每个最终事件保留最多 2 条备用来源用于正文抓取和交叉核验。
- [x] 保持最终候选计数按事件计数，不把互证来源重复展示到日报。
- [x] 修复页面标题证据被错误判为未落地。
- [x] 增强融资规则降级的企业、日期、轮次和金额证据提取。
- [x] 增加两个独立 B 级正文在 AI 降级时仍可严格核验通过的测试。
- [x] 完整离线测试通过：391 passed。
- [ ] 在强制 dry-run 分支执行一次真实验证，确认真实候选中是否存在可用 A 级或两个独立 B 级来源。

## 已完成的安全验证

### 采集恢复

- [x] 修复博查包装响应解析。
- [x] 检查博查 HTTP 业务错误码。
- [x] 保持 4 查询上限。
- [x] 保持 `dry_run=true`。
- [x] 完成三次一次性安全验证。
- [x] 确认真实博查候选不再为 0。
- [x] 确认不关闭 TLS 校验。

### 候选可见性

- [x] `PendingItem` 保留搜索摘要。
- [x] 正文抓取失败后展示候选标题、摘要和原始链接。
- [x] 报告数据完整性增加候选数量。
- [x] 本地完整测试通过。
- [x] 创建 PR #4。
- [ ] PR #4 尚未合并。

## 本轮“有无信息”开发

设计文档：

- `docs/superpowers/specs/2026-07-27-information-availability-prd.md`
- `docs/superpowers/plans/2026-07-27-information-availability.md`

当前开发分支：

```text
codex/information-availability
```

已实现：

- [x] 四个核心查询增加明确分类提示。
- [x] 商业航天融资查询移除采购、招标和中标词。
- [x] 博查 `freshness` 从 `noLimit` 调整为 `oneMonth`。
- [x] 解析并独立保存 `datePublished`。
- [x] 无时区、无效或缺失日期不再伪装为当天日期。
- [x] 增加标题、摘要和 HTTP(S) URL 结构门槛。
- [x] 增加四板块确定性主题锚点。
- [x] 商业航天融资使用“航天主体＋融资事件”双锚点。
- [x] 排除打印机、硒鼓、墨盒、雕刻、切割、美容、医美、通用医疗用品和无关 AI 软件采购。
- [x] 增加 0–7 天、8–30 天和日期未知分桶。
- [x] 近 7 天不足 5 条时才用 8–30 天补足。
- [x] 日期未知候选最多补充 2 条。
- [x] 最终候选最多 10 条。
- [x] 搜索候选在正文抓取前完成过滤。
- [x] 正文抓取失败保留板块、来源日期、摘要和链接。
- [x] 报告展示近 7 天、8–30 天补充和日期未知标签。
- [x] 报告增加采集漏斗和“信息可用/信息不足”判定。
- [x] `RunResult` 保留通过搜索门槛的候选，避免下游拒绝后从当次日报完全消失。
- [x] “今日重点跟进”展示未进入正式数据或 `pending` 的“搜索候选（未核实）”，包含板块、来源日期、时间标签、标题、摘要和链接。
- [x] 已进入正式数据或 `pending` 的 URL 不重复展示为搜索候选。
- [x] 新增 21 条固定样本，覆盖近期、补充、未知日期、旧闻、噪声和重复 URL。

采集漏斗字段：

```text
raw_search_count
valid_shape_count
relevance_pass_count
recent_7d_count
fallback_8_30d_count
unknown_date_count
final_candidate_count
fetch_failure_count
information_available
```

## 智能多轮检索开发

设计与计划：

- `docs/superpowers/specs/2026-07-28-agentic-intelligence-retrieval-design.md`
- `docs/superpowers/plans/2026-07-28-agentic-intelligence-retrieval.md`

开发分支：

```text
codex/agentic-intelligence-retrieval
```

已实现：

- [x] DeepSeek `search_web` Tool Calling 编排器。
- [x] 四板块确定性种子搜索。
- [x] 日常 12 次、历史回填 40 次硬预算。
- [x] 重复查询拦截、轮次上限和连续无新增停止。
- [x] DeepSeek 规划失败后保留种子搜索结果并标注降级。
- [x] 日常 `oneMonth`、历史回填 `oneYear` 博查时间范围；本地回填候选限制为近 90 天。
- [x] 研报销售页、股市评论、荐股和泛化券商观点排除。
- [x] 扩展排除“发展深度洞察”“产业发展报告”“报告摘要”等研报变体，并在抓取正文前屏蔽 IIM/中研网报告页。
- [x] 排除投融周报、融资周报及融资盘点等非单一事件页面。
- [x] 相同企业、相同融资轮次、发布日期相近的跨媒体线索进行事件级合并。
- [x] 拦截激光粒度分析仪、医疗激光等智能体误查询，且不消耗搜索预算。
- [x] 修复历史回填分类候选仍按 30 天显示的问题，统一展示“8–90 天补充”。
- [x] 回填查询自动追加准确的 90 天起止日期。
- [x] DeepSeek 只接收时间窗口内搜索结果作为后续检索上下文，窗口外结果不再诱导追查旧项目。
- [x] 工具结果返回窗口外数量，供模型调整检索方向。
- [x] 智能检索轮次上限由 4 调整为 8，搜索预算硬上限仍保持日常 12、回填 40。
- [x] 商业航天融资候选标题必须明确包含融资、投资或增资动作，排除新闻早/晚报和产业生态综述。
- [x] 对标题明确、含具体企业且正文具有航天/火箭/卫星业务证据的融资放宽通用主体词要求，避免误删微光启航等单一事件。
- [x] 连续多轮融资标题解析为轮次集合；同企业、日期相近且轮次集合重叠的跨媒体报道合并。
- [x] 融资企业归一化去除常见宣传前缀、注册地和法律主体后缀，修复微光启航同轮融资重复。
- [x] 采购类主题与生命周期双门槛。
- [x] 普通页与打印页事件级合并，不合并不同生命周期事件。
- [x] 分类板块展示“候选线索（未核实）”，不再在“今日重点跟进”重复展示。
- [x] 报告展示搜索预算、实际调用、模型轮次、重复查询、事件过滤、事件合并和停止原因。
- [x] Artifact 保存不含敏感信息的 `data/research-trace.json`。
- [x] CLI 支持 `daily`、`backfill` 和“【测试】”标题。
- [x] workflow 仍只有手动入口并强制 dry-run。

验证状态：

- [x] 40 查询历史回填真实 dry-run。
- [ ] 12 查询日常真实 dry-run。
- [ ] 新版受控钉钉测试发送。
- [ ] 新分支 PR 创建或任何 PR 合并。

## 测试结果

```text
发现层与模型测试：70 passed
发现/流水线/报告/固定样本定向测试：158 passed
候选展示修复定向测试：108 passed
完整离线测试：385 passed
git diff --check：通过
```

尚未验证：

- [x] 博查真实 API 接受 `freshness=oneMonth`。
- [x] 一次真实 4 查询得到 5 条最终候选。
- [x] 真实候选中未出现打印机、美容等原有明显噪声。
- [x] 修复后的真实消息能够展示 5 条候选。
- [x] 离线排除行业研究报告销售页、股市评论、投融周报和泛化券商观点。
- [x] 离线合并同企业同轮次的跨媒体融资候选。
- [ ] 真实 DeepSeek 分析与核验率。

## 当前 GitHub 状态

```text
origin/main：eef32c8
PR #3：merged
PR #4：https://github.com/waerjili123/daily-aerospace/pull/4
PR #4 状态：open / not merged
PR #4 分支：codex/pending-candidate-report

本地开发分支：codex/information-availability
本地开发基线：包含 PR #4 提交 c150877
PR #5：https://github.com/waerjili123/daily-aerospace/pull/5
PR #5 状态：open / not merged

智能检索开发分支：codex/agentic-intelligence-retrieval
智能检索 PR：尚未创建
```

## 下一步

1. 创建一次性 12 查询钉钉测试分支；
2. 执行一次标题含“【测试】”的受控钉钉发送；
3. 发送后立即将测试分支恢复为强制 dry-run；
4. 再决定 PR #4、PR #5 与智能检索分支的合并顺序。

以下事项仍未授权：

- 不合并 PR #4；
- 仅允许按已批准设计执行一次 12 查询、标题含“【测试】”的受控钉钉验证；
- 不修改 Secrets；
- 不修改仓库可见性；
- 不恢复或启用 schedule；
- 不暂停或启用 workflow。

## 2026-07-28 核验升级进展

开发分支：

```text
codex/verification-promotion-20260728
```

第 12 次 Actions 日常真实 dry-run 已完成：

- [x] Actions 成功，耗时 3 分 44 秒，未发送钉钉。
- [x] 博查原始 95 条、结构有效 95 条、主题相关 14 条、最终候选 5 条。
- [x] 智能检索预算 12 次，实际调用 12 次，共 2 轮。
- [x] 正文抓取失败 0。
- [x] 多来源保留生效：3 个融资事件的补充来源进入 `pending`。
- [ ] 已核实事件仍为 0。
- [ ] `pending` 3 条均因 `missing_required_fields` 未升级。

第 13 次 Actions 日常真实 dry-run（提交 `3d2939d`）：

- [x] Actions 成功，耗时 3 分 17 秒，未发送钉钉。
- [x] 最终候选 5 条，正文抓取失败 0。
- [x] `pending` 从 3 条降为 2 条，证明保守字段补全已进入真实流水线。
- [ ] 微光启航、谱星航天仍显示笼统的 `missing_required_fields`。
- [x] 后续报告将直接列明缺失的是标题、主体、发布日期、分类或事件类型，避免继续盲查。

第 14 次 Actions 日常真实 dry-run（提交 `4600755`）：

- [x] Actions 成功，耗时 2 分 52 秒，未发送钉钉。
- [x] 唯一待核实候选为光邮星空，精确缺口为“发布日期”。
- [x] 已确认主体、分类和事件类型提取不再是该候选的阻塞项。
- [x] 抓取器新增网页自身发布时间元数据保留，支持
  `article:published_time`、`datePublished` 和 JSON-LD。
- [x] 不采用博查搜索日期作为核验事实；只有目标网页自身声明的日期才进入正文证据。
- [x] 完整离线测试：401 passed。

第 15 次 Actions 日常真实 dry-run（提交 `ff390a3`）：

- [x] Actions 成功，耗时约 3 分 18 秒，未发送钉钉。
- [x] 博查原始 101 条、主题相关 16 条、最终候选 5 条、正文抓取失败 0。
- [x] 待核实 5 条；原因已按字段和来源门槛明确展示。
- [x] 龙擎空天（腾讯来源）已越过字段完整性检查，推进到
  `financing_requires_official_or_two_independent_b_sources`。
- [x] 证明抓取、融资字段补全和页面发布时间元数据链路已在真实环境生效。
- [ ] 已核实仍为 0；当前主瓶颈已转为官方来源或两个独立 B 级来源不足。
- [ ] 中宏网、凤凰网和投中网页面自身未提供当前抓取器可核验的发布日期，
  继续保守标为日期缺失，不采用博查日期代替。
- [ ] 光邮星空已具备日期，但分类证据仍未通过严格核验。

下一开发重点应从扩大候选数转为：

1. 优先搜索融资企业官方公众号/官网或投资机构官方披露；
2. 对同一事件定向补齐第二个独立 B 级来源；
3. 保持 C 级转载只能作为线索，不能单独升级为已核实；
4. 修复 dry-run 末尾仓库写回产生的非阻断 `git exit 128` 警告。

已确认的新根因：

- DeepSeek 可能返回通过结构和证据校验、但主体、日期、金额或轮次为空的结果。
- 旧逻辑仅在主模型彻底失败时调用规则分析器，因此“合法但不完整”的模型结果不会获得确定性补全。
- 投资界和投中网页面正文实际包含融资主体、日期、金额或轮次，字段缺失并非来源页面没有信息。

本轮修复：

- [x] 模型结果有效但字段为空时，使用同一页面的确定性结果补空。
- [x] 只补空字段，不覆盖任何模型已有非空事实。
- [x] 模型判定不在范围内，或模型与规则分类冲突时，不执行补全。
- [x] 补入字段时同步合并逐字证据，并标记为降级分析。
- [x] 融资主体提取兼容公司简称括号、`已于近日`、`于近期`、`连续完成` 等真实新闻句式。
- [x] 完整离线测试：397 passed。

下一验证步骤：

1. 推送本分支；
2. 仅手动触发一次强制 `dry-run` 的 12 查询日常任务；
3. 检查 `pending` 原因是否从字段缺失转为来源等级/独立来源不足，或是否出现首条严格核实事件；
4. 未经确认不发送钉钉、不修改 Secrets、不启用 schedule、不合并 PR。
## 2026-07-29 双通道滚动核验池

已批准并实现：

- [x] 发现通道保持日常 12 次查询预算。
- [x] 核验通道只在存在严格待核实对象时启用，最多额外调用 3 次，总硬上限 15 次。
- [x] 待核实对象采用 90 天滚动池，记录核验次数、已尝试查询、连续无新增来源次数和最后核验时间。
- [x] 核验对象按“更接近严格核验成功、更新、尝试次数更少”排序。
- [x] 定向搜索优先寻找融资主体/投资机构官方披露或独立 B 级媒体，以及采购官方项目页和状态页。
- [x] 定向搜索结果进入同一抓取、分析、事件合并和严格核验链路；没有降低来源等级或字段完整性要求。
- [x] 报告新增发现/核验通道调用、定向核验对象、新增来源、重复来源和触发原因指标。
- [x] 配置、状态模型、预算边界、停止条件、查询去重、流水线再次核验及报告均有离线测试。
- [x] 完整离线测试：409 passed。
- [x] `git diff --check`：通过。
- [ ] 当前分支真实 dry-run：待执行。
- [ ] 至少产生 1 条严格“已核实”信息：待真实 dry-run 验证。

安全边界：

- workflow 仍仅允许手动触发并强制 dry-run；
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置；
- 未发送钉钉，未合并任何 PR。

## 2026-07-29 运行 #16 与分类证据修复

运行 #16（提交 `e75f33c`）真实 dry-run：

- [x] 分支和提交正确，Actions 成功，耗时 3 分 6 秒，未发送钉钉。
- [x] 基础调用 12、弹性调用 3、总调用 15，硬上限生效。
- [x] 定向核验处理 1 个事件，新增来源 1，重复来源 29。
- [x] 博查原始 80、主题相关 10、最终候选 4、正文抓取失败 0。
- [ ] 严格已核实 0；待核实 3。
- [x] 主要阻断定位为 `classification_evidence_invalid` 和目标自身域名重复查询。

本轮修复：

- [x] 同一页面内的类别证据和事件证据允许由不同原文引文共同证明。
- [x] 仍要求页面级确定性分类与主分析一致，且类别和事件证据均存在。
- [x] 单一 B 级来源仍保持待核实，不降低严格来源门槛。
- [x] 融资定向核验排除目标自身 B 级域名及其 `www`/子域名。
- [x] 排除后没有足够独立域名时，不生成重复查询填满弹性预算。
- [x] 定向测试：218 passed。
- [x] 完整离线测试：415 passed。
- [ ] 修复后的真实 dry-run：待执行。
## 2026-07-29 运行 #17 与页面级组合判定修正

运行 #17（提交 `c5d4f44`）真实 dry-run：

- [x] Actions 成功，耗时 4 分 10 秒，未发送钉钉。
- [x] 基础调用 12、弹性调用 3、总调用 15。
- [x] 博查原始 86、主题相关 16、最终候选 5、正文抓取失败 0。
- [x] 目标自身域名排除逻辑已进入真实运行。
- [ ] 定向核验新增来源 0、重复来源 30、严格已核实 0。
- [ ] 光邮星空和谱星航天仍为 `classification_evidence_invalid`。

进一步核对发现：原实现虽然汇总了同页证据，却仍先逐条要求 `category` 引文单独
返回商业航天融资类别。“星地激光通信”单条引文会被规则识别为激光通信，只有
与另一条融资引文组合后才能准确表达“商业航天融资事件”。

修正：

- [x] 使用同一页面 `in_scope`、`category`、`event_type` 引文的组合文本判断
  页面级类别和事件类型。
- [x] 仍要求页面确定性分析与主分析一致，不允许跨页面拼接分类证据。
- [x] 新增“星地激光通信业务引文 + 融资动作引文”的真实语义回归测试。
- [x] 单一 B 级来源仍保持待核实。
- [x] 完整离线测试：416 passed。
- [ ] 修正后的真实 dry-run：待单独授权后执行。

## 2026-07-29 运行 #18 与严格核验证据重建

运行 #18（提交 `cfd0e75`）真实 dry-run：

- [x] Actions 成功，运行时间为北京时间 14:36—14:40，未发送钉钉。
- [x] 基础调用 12、弹性调用 3、总调用 15。
- [x] 博查原始 90、主题相关 17、最终候选 5、正文抓取失败 1。
- [ ] 严格已核实 0；待核实 3。
- [ ] 光邮星空仍为 `classification_evidence_invalid`。
- [ ] 定向核验新增来源 0、重复来源 30；三次弹性查询没有补入独立来源。
- [x] 云幕智造样本暴露“只有创始人航天履历、企业主营业务并非商业航天”的
  融资误匹配。
- [x] 微光启航因目标网页缺少可核验发布日期保持待核实，未使用搜索日期替代。

已确认的设计与计划：

- `docs/superpowers/specs/2026-07-29-strict-verification-evidence-reconstruction-design.md`
- `docs/superpowers/plans/2026-07-29-strict-verification-evidence-reconstruction.md`

本轮实现：

- [x] 规则分析器分别选择最小的业务类别引文和融资动作引文，不再把整篇正文
  保存为单条分类证据。
- [x] 主模型与确定性分析结论一致时，合并同一页面的确定性分类证据；只补证据，
  不覆盖模型已有的非空事实。
- [x] 商业航天融资分类兼容“空间激光通信”业务表述。
- [x] 分类证据失败拆分为境内主体、目标业务、事件动作和范围证据四类原因。
- [x] 报告为四类失败原因提供简短中文说明，新的原因可以进入 90 天核验池。
- [x] 商业航天融资相关性不再允许泛化“航天背景/航天基因”单独通过；仍接受
  火箭、卫星、航天器、星载和空间激光通信等明确业务。
- [x] 弹性查询调整为官方披露、投资方交叉查询和开放式新闻查询，不再默认固定
  `stcn.com`/`cls.cn`。
- [x] 根据中关村科学城公司官网的光邮星空 Pre-A+ 轮投资公告，将
  `zgccity.com` 以“北京中关村科学城创新发展有限公司/中关村科学城公司”
  精确别名登记为官方投资方域名；仍须网页投资方字段和逐字证据同时匹配。
- [x] 两个独立 B 级“空间激光通信业务 + 融资动作”页面的端到端核验回归通过。
- [x] 定向测试：345 passed。
- [x] 完整离线测试：427 passed。
- [x] `git diff --check`：通过。
- [ ] 修复后的真实 dry-run：待推送后由用户手动触发。
- [ ] 至少产生 1 条严格“已核实”信息：待真实 Artifact 验证。

安全边界保持不变：

- workflow 仍仅允许手动触发并强制 dry-run；
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置；
- 未发送钉钉，未合并任何 PR。

## 2026-07-29 弹性核验预算动态分配

最新真实 dry-run Artifact（报告时点 2026-07-29 17:02）：

- [x] 基础调用 12、弹性调用 3、总调用 15，预算硬上限正常生效。
- [x] 博查原始 80、结构有效 80、主题相关 13、最终候选 4。
- [ ] 严格已核实仍为 0；`financings.json` 仍为空。
- [x] 待核实 2 条：微光启航缺少目标网页可核验发布日期；谱星航天缺少境内主体证据。
- [x] 三次弹性查询全部分配给谱星航天，触发原因均为
  `classification_country_evidence_invalid`。
- [x] 光邮星空仅保留腾讯新闻候选，没有进入弹性核验；已登记的中关村科学城官方投资页面
  未进入抓取和严格核验链路。

确认根因：

- 核验规划器由配置限制为 `verification_max_targets: 1`。
- 排序把 B 级媒体来源置于较新的 C 级线索之前，但没有计算“已知官方企业/投资方域名可命中”
  的晋升潜力。
- 三次查询在搜索前一次性规划，无法在每次新增来源抓取和重新核验后动态转移剩余预算。
- 当前瓶颈是预算分配而不是弹性调用次数不足，因此继续保持基础 12、弹性 3、总上限 15。

已确认设计与计划：

- `docs/superpowers/specs/2026-07-29-elastic-verification-budget-allocation-design.md`
- `docs/superpowers/plans/2026-07-29-elastic-verification-budget-allocation.md`

本轮实现：

- [x] 生产配置把单次运行可覆盖的核验目标从 1 调整为最多 3；弹性调用总数仍最多为 3。
- [x] 规划器改为逐次规划：第一目标、不同的第二目标、第三次按最新状态动态回补。
- [x] 同一融资事件的多个来源按主体、类别、事件类型和轮次合并为一个核验目标，不会挤掉
  第二个事件。
- [x] 晋升排序优先考虑已登记官方企业或投资方域名可命中、字段完整度、来源缺口、来源等级、
  历史尝试次数和发布日期。
- [x] 已知官方域名和主体/投资方别名匹配时，首个查询使用精确 `site:` 定向搜索。
- [x] 每次弹性查询后立即执行候选筛选、抓取、分析、同事件来源合并和严格重新核验；事件已经
  严格通过时不再继续消耗弹性预算。
- [x] 匹配官方域名的结果即使位于普通候选裁剪上限之外，只要仍包含目标主体和融资动作，
  也会进入抓取；域名命中本身不会绕过正文证据校验。
- [x] 研究轨迹新增分配序号、分配原因、查询后核验状态和精确核验原因。
- [x] pending 的连续无新增来源计数改为按目标事件更新，不再被其他事件的新来源错误重置。
- [x] 定向测试：89 passed。
- [x] 完整离线测试：432 passed。
- [x] `git diff --check`：通过。

安全边界保持不变：

- workflow 仍仅允许手动触发并强制 `--dry-run`。
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置。
- 未发送钉钉，未合并任何 PR。

- 下一次真实 dry-run 仍需在推送开发分支后由用户手动触发，并以 Artifact 至少 1 条严格
  `verified` 作为业务验收；Actions success 不能替代该验收。

## 2026-07-30 官方搜索线索与无新增停止

最新真实 dry-run Artifact（报告时点 2026-07-30 08:17）：

- [x] 基础调用 12、弹性调用 3、总调用 15，预算硬上限正常生效。
- [x] 博查原始 96、主题相关 15、最终候选 5、待核实 4。
- [ ] 严格已核实仍为 0。
- [x] 三次弹性查询均分配给光邮星空，但新增独立来源均为 0。
- [x] 候选摘要包含“中关村科学城”，查询却未生成 `site:zgccity.com`。
- [x] 光邮星空目标页包含“北京光邮星空科技有限公司”，但仍因
  `classification_country_evidence_invalid` 未晋升。
- [x] 同一目标第二、三次查询被错误标记为 `cover_distinct_target`，且连续两次
  无新增后仍执行第三次。

已确认设计与计划：

- `docs/superpowers/specs/2026-07-30-official-clue-and-verification-stop-design.md`
- `docs/superpowers/plans/2026-07-30-official-clue-and-verification-stop.md`

本轮实现：

- [x] 新增确定性官方搜索线索提取：按结构化字段、同页逐字证据、候选标题/摘要三层匹配
  配置中登记的企业和投资方完整别名。
- [x] 标题和摘要仅用于决定搜索方向，不写回结构化事实，也不能直接改变核验状态。
- [x] 摘要包含“中关村科学城”且投资方字段为空时，仍能生成
  `site:zgccity.com` 定向查询。
- [x] 同页明确出现“北京光邮星空科技有限公司”时，优先生成该逐字
  `in_china` 证据；仍要求主分析和确定性分析结论一致。
- [x] 连续无新增计数实时反馈给逐次规划器；达到 2 次后停止该目标并保留未使用预算，
  或把预算转移给其他合格事件。
- [x] 历史 pending 已累计 1 次无新增时，本次只允许再尝试 1 次；研究轨迹立即记录
  `no_new_source_threshold`。
- [x] 分配标签修正：只有实际切换事件才使用 `cover_distinct_target`，同一事件重试使用
  `retry_same_target`。
- [x] 研究轨迹新增官方域名、命中别名、线索层级和连续无新增计数。
- [x] 两个独立 B 级融资页面在主模型缺少境内证据时，可由同页逐字证据补全并继续通过
  既有严格核验端到端回归；未降低来源或字段门槛。
- [x] 定向测试：220 passed。
- [x] 完整离线测试：438 passed。
- [ ] 修复后的真实 dry-run：待推送后由用户手动触发。
- [ ] 至少产生 1 条严格“已核实”信息：待真实 Artifact 验证。

安全边界保持不变：

- workflow 仍仅允许手动触发并强制 `--dry-run`。
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置。
- 未发送钉钉，未合并任何 PR。

## 2026-07-30 融资证据缺口定向核验

最新真实 dry-run Artifact（报告时点 2026-07-30 11:08）：

- [x] 基础检索调用 12 次。
- [ ] 弹性核验调用 0 次；总调用只有 12 次，预留的 3 次没有进入规划。
- [x] 博查原始 95、主题相关 15、最终候选 5、待核实 4。
- [ ] 严格已核实仍为 0；`financings.json` 和 `events.jsonl` 为空。
- [x] 光邮星空投资界页面已不再报境内分类证据错误，推进到
  `financing_missing_required_evidence`。
- [x] 该原因未在弹性核验允许集合中，导致 `site:zgccity.com` 和其他证据补充
  查询均未执行。
- [x] 页面同时包含“北京光邮星空科技有限公司”和“北京邮电大学”时，原规则会
  错误选择更短的大学名称作为境内主体证据。

已确认设计与计划：

- `docs/superpowers/specs/2026-07-30-financing-evidence-gap-followup-design.md`
- `docs/superpowers/plans/2026-07-30-financing-evidence-gap-followup.md`

本轮实现：

- [x] 新增无副作用的融资证据缺口分类，覆盖主体、日期、金额、轮次、融资子类型和
  已有投资方的逐字证据。
- [x] 金额严格区分“明确披露”“明确未披露”和“页面单纯省略”；单纯省略仍是
  `amount` 缺口，不能晋升。
- [x] `financing_missing_required_evidence` 现在可以进入 90 天弹性核验池。
- [x] 弹性计划附带 `missing_evidence_fields`，研究轨迹可直接看到具体缺口。
- [x] 金额缺口查询增加“融资金额、金额未披露、具体金额”关键词。
- [x] 光邮星空摘要命中“中关村科学城”时，首个查询同时包含金额缺口词和
  `site:zgccity.com`。
- [x] 境内主体按企业、采购/军队单位、研究机构、大学排序；同页企业优先于
  北京邮电大学。
- [x] 标题和摘要仍只决定搜索方向，不写回金额、日期、投资方或核验状态。
- [x] pending 顶层原因、连续无新增停止、12+3 预算和严格来源门槛保持不变。
- [x] 定向测试：224 passed。
- [x] 完整离线测试：442 passed。
- [x] `git diff --check`：通过。
- [ ] 修复后的真实 dry-run：待推送后由用户手动触发。
- [ ] 至少产生 1 条严格“已核实”信息：待真实 Artifact 验证。

安全边界保持不变：

- workflow 仍仅允许手动触发并强制 `--dry-run`。
- 钉钉 webhook 仍为无效占位地址。
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置。
- 未发送钉钉，未合并任何 PR。

## 2026-07-30 候选诊断与发布日期定向核验

真实 dry-run 运行 #22（提交 `3ca57e6`，报告时点 2026-07-30 16:30）：

- [x] 确认运行分支为 `codex/verification-promotion-20260728`，提交正确。
- [x] 基础检索 12 次，博查原始 97、主题相关 10、最终候选 4。
- [ ] 弹性核验 0 次，严格已核实 0，待核实 2。
- [x] 两条 pending 均为微光启航，原因是
  `missing_required_fields:published_at`。
- [x] 光邮星空腾讯候选出现在报告中，但原 Artifact 没有记录其未进入 pending 和
  弹性核验的具体原因。
- [x] dry-run 从仓库空状态启动，上一轮 Artifact pending 不会自动成为下一轮输入。

已确认设计与计划：

- `docs/superpowers/specs/2026-07-30-candidate-diagnostics-and-date-followup-design.md`
- `docs/superpowers/plans/2026-07-30-candidate-diagnostics-and-date-followup.md`

本轮实现：

- [x] 新增统一、可解释的弹性资格结果，规划器和候选诊断共用相同判断。
- [x] 精确允许 `missing_required_fields:published_at` 进入 90 天弹性核验池。
- [x] 候选搜索日期只用于滚动池资格和排序，不写回
  `AnalysisResult.published_at`。
- [x] 日期缺口查询加入“发布日期、发布时间、公告时间、官方披露”。
- [x] 同时缺主体或类别、池外日期、非 pending、原因不支持和无新增阈值均有明确
  不合格原因。
- [x] 新增精简候选诊断，覆盖抓取失败、分析失败、核验拒绝、pending、verified 和
  后置 payload 校验失败。
- [x] 诊断记录来源、是否为报告候选、阶段、状态、原因、来源等级、缺失字段、首次
  弹性资格及是否已尝试。
- [x] 诊断 URL 自动移除查询参数和片段；不保存正文、异常消息、模型原始输出、
  Secrets 或 webhook。
- [x] CLI 原子写入 UTF-8 `data/candidate-diagnostics.json`；现有 Artifact 上传
  已覆盖整个 `data/` 目录。
- [x] 定向测试：146 passed。
- [x] 完整离线测试：446 passed。
- [x] `git diff --check`：通过。
- [ ] 修复后的真实 dry-run：待推送后由用户手动触发。
- [ ] 至少产生 1 条严格“已核实”信息：待真实 Artifact 验证。

安全边界保持不变：

- workflow 仍仅允许手动触发并强制 `--dry-run`。
- 钉钉 webhook 仍为无效占位地址。
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置。
- 未发送钉钉，未合并任何 PR。

## 2026-07-30 页面证据与严格来源修复

最新真实 dry-run Artifact（报告时点 2026-07-30 18:50）：

- [x] 基础检索 12 次、弹性核验 3 次、总计 15 次，硬上限正常生效。
- [x] 博查原始 99、结构有效 99、主题相关 22、最终候选 5、正文抓取失败 0。
- [ ] 严格已核实仍为 0；待核实 5，`events` 和 `financings` 仍为空。
- [x] 候选诊断共 10 条：5 条 pending、5 条因 `out_of_scope` 拒绝。
- [x] 三次弹性调用都用于光邮星空同一事件：第一次新增 1 个来源，后两次新增 0；
  另有 3 个合格事件未获得尝试。
- [x] 投中网、顶端新闻和剑鱼标讯页面请求均返回成功，但正文抽取结果遗漏网页标题区
  可见的完整发布日期。
- [x] 微光启航国内融资正文后部提到 SpaceX，旧规则因扫描全文而把国内主体误判为境外。

已确认设计与计划：

- `docs/superpowers/specs/2026-07-30-strict-verification-source-evidence-repair-design.md`
- `docs/superpowers/plans/2026-07-30-strict-verification-source-evidence-repair.md`

本轮实现：

- [x] `FetchedPage` 增加发布日期逐字证据和
  `metadata` / `json_ld` / `visible_header` 来源类型。
- [x] 可见日期只从明确发布日期标签、文章标题区和署名区提取；更新时间、页脚、
  相关推荐和正文历史日期均不作为发布时间。
- [x] 境内外判断改为标题和事件主体上下文；国内主体不会再被正文后部 SpaceX
  比较对象否决，真正境外主体仍保持排除。
- [x] 融资核验目标采用企业结构化别名、类别、事件类型、轮次集合和 7 天日期邻近
  规则生成稳定事件键；全称和简称可以归并，不同轮次或相距超过 7 天保持独立。
- [x] 三次弹性预算在存在三个合格事件时先各覆盖一次，之后才允许同一事件重试。
- [x] 候选选择按官方来源、已登记 B 级来源、其他来源排序；较新的 C 级转载不再
  挤掉可用于严格互证的 B 级页面。
- [x] 只新增 `chinaventure.com.cn` 为融资 B 级来源；未新增东方财富、新浪、
  腾讯或其他转载域名。
- [x] 单一投中网 B 级页面仍为 pending；投中网与投资界两个独立 B 级页面在
  主体、日期、轮次和金额一致时，离线严格晋级为
  `verified_financing_two_independent_sources`。
- [x] 两个 B 级页面金额冲突时仍保持 pending。
- [x] 候选诊断新增 `publication_date_source` 和
  `verification_event_key`，未新增正文、模型原始输出或敏感信息。
- [x] 定向测试：406 passed。
- [x] 完整离线测试：465 passed。
- [x] `git diff --check`：通过。
- [x] workflow 仍仅有 `workflow_dispatch`，并强制 `--dry-run`。
- [x] 日常基础 12、弹性 3、总硬上限 15 均未改变。
- [ ] 修复后的真实 dry-run：待提交、取得公开分支推送授权并由用户手动触发。
- [ ] 至少产生 1 条真实严格“已核实”信息：仍须下一份 Artifact 验收。

安全边界保持不变：

- 未触发 Actions，未发送钉钉。
- 未修改 Secrets、仓库可见性或 workflow 启停/定时配置。
- 未合并任何 PR，未执行 `dry_run=false`。

## 2026-07-30 Actions #24 失败诊断与产物隔离

Actions #24（提交 `1b35fdc`，分支
`codex/verification-promotion-20260728`）：

- [x] 离线测试和 dry-run 分支保护步骤均已通过。
- [x] `Run daily pipeline` 在运行约 2 分 35 秒后因 `TypeError` 退出，CLI
  返回既有流水线错误码 4。
- [x] Node.js 20 弃用提示和 checkout 后处理的 Git 128 是警告，不是本次
  流水线失败的根因。
- [x] 页面显示的 2026-07-26 日报摘要和 Artifact 中的旧报告/数据来自旧
  workflow 的 `if: always()` 兜底上传，不是 #24 生成的新日报。
- [x] 旧 CLI 只记录异常类型，未记录安全的阶段和代码位置，因此仅凭 #24
  现有日志还不能确定 `TypeError` 的具体代码根因。

本轮本地修复：

- [x] CLI 在失败时原子写入脱敏的 `data/failure-diagnostics.json`，只包含稳定
  阶段、异常类型、发生时间和仓库内 Python 栈帧，不包含异常消息、局部变量、
  请求内容、模型输出或环境值。
- [x] CLI 在成功时写入 `data/run-result.json`，明确记录本次实际生成的日报路径；
  成功与失败元数据互斥，并在每次运行开始时清理旧元数据。
- [x] workflow 成功时只按 `run-result.json` 展示本次日报并上传
  `daily-intelligence-report`，不再扫描目录选取历史最新文件。
- [x] workflow 失败时只展示脱敏诊断并上传
  `daily-intelligence-failure-diagnostics`；若失败发生在诊断文件生成前，则明确提示
  查看失败步骤日志，不回退到旧日报。
- [x] 既有退出码保持不变：配置 2、通知 3、流水线 4。
- [x] CLI 与 workflow 聚焦回归：101 passed。
- [x] 完整离线测试：475 passed。
- [ ] `TypeError` 的业务根因仍需在本修复推送后，通过下一次手动 dry-run 的阶段与
  仓库栈帧继续定位；本轮没有把诊断增强误当作根因修复。

安全边界保持不变：

- workflow 仍仅有 `workflow_dispatch`，并强制 `--dry-run`、
  `--discovery-mode daily`、`--max-queries 12`。
- 日常基础 12、弹性最多 3、总硬上限 15 均未改变。
- 本地开发期间未触发 Actions、未发送钉钉、未推送分支。
- 未修改 Secrets、仓库可见性、workflow 启停/定时配置或 `dry_run` 策略。
- 未合并任何 PR。

## 2026-07-31 可直接钉钉推送版本

已批准设计：

- `docs/superpowers/specs/2026-07-31-dingtalk-ready-intelligence-report-design.md`
- `docs/superpowers/plans/2026-07-31-dingtalk-ready-intelligence-report.md`

本轮实现：

- [x] 模型日期与页面元数据、JSON-LD 或可见页头日期一致时，补入逐字
  `published_at` 证据；冲突时不覆盖。
- [x] 融资主体清洗行业前后缀和新闻话术，避免描述性短语成为企业名称。
- [x] 同一融资主体、相容轮次及事实在 30 天内归为同一核验事件；其他事件继续使用
  7 天窗口。
- [x] 弹性核验优先搜索尚未使用的注册 B 级媒体域名；基础 12、弹性 3、总上限
  15 不变。
- [x] 日报新增“今日最值得看”，分类板块直接显示严格已核实、高可信待核实和普通
  候选，并按核验事件键去重。
- [x] 分析完成后原子写入本轮候选检查点；后续失败生成“【降级】”快报，检查点前
  失败只生成“【异常】”告警。
- [x] `delivery-status.json` 只记录 `skipped`、`accepted` 或 `failed` 及报告类型，
  不包含 webhook、签名和 Secret。
- [x] 手动 workflow 增加 `delivery_mode`，默认 `dry_run`；只有
  `dingtalk_test` 使用现有 Secrets 并添加“【测试】”标题。
- [x] workflow 仍仅含 `workflow_dispatch`，不提交状态，不启用定时任务。
- [x] 完整离线测试扩展到 482 项。
- [x] 推送分支并执行 Actions #26 dry-run；流程成功并产生 1 条严格已核实融资
  事件，但报告语义验收未通过，因此未发送钉钉。
- [x] 修复 #26 暴露的报告问题：历史事件不再进入“过去24小时”；超过日报时窗但
  本轮新核实的内容进入“本轮新核实/历史补录”；同一事件只在优先级最高的一个
  板块展示。
- [x] 融资金额、投资方和领域只在来源明确披露时显示；仅当金额证据逐字出现
  “未披露/未公布/未透露”时才显示“金额：未披露”。
- [x] 多个无法识别类型的来源链接显示为“来源1、来源2”，不再出现
  “来源 / 来源”。
- [x] 完整离线测试扩展到 485 项，`git diff --check` 通过。
- [ ] 推送报告语义修复并执行下一次真实 dry-run。
- [ ] dry-run 达到至少 1 条严格已核实后，执行一次真实钉钉测试推送。
